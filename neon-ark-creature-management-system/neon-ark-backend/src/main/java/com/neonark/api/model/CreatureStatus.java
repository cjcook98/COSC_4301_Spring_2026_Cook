package com.neonark.api.model;

// Enum representing health status of a creature.
// Used to enforce valid status values.
public enum CreatureStatus {
    Healthy,
    Injured,
    Critical,
    Removed
}
